from typing import List, Union
from .setup import Server

def create_inventory(server: Union[List[Server], Server]) -> str:
  # 임시 파일 위치 반환
  pass

def run_playbook(inven: str, roles: List[str]) -> bool:
  # 임시 playbook.yml 만들어서 실행
  pass