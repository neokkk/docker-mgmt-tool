import ServerList from '../components/ServerList';
import Layout from '../components/layout';

const Root = () => {
  return (
    <Layout>
      <p>Root</p>
      <ServerList />
    </Layout>
  );
};

export default Root;
